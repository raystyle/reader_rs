---
id: REQ-064
title: 封版0110
status: implemented
priority: must
trace: Cargo.toml（版本载体唯一权威 0.11.0）加 Cargo.lock（随版本重生）加 CHANGELOG.md（Unreleased 定版 0.11.0，三 REQ 条目）加 tests/snapshots/snapshot__llms_index.snap 与 docs/aidoc/（版本行随出）加 dev/v0.11.0 分支（D45 承载，FF 合并 main 打 tag v0.11.0 触发 release.yml）；验收记录见 diary 当日
---

# REQ-064:封版0110

## Scenario

Unreleased 有货三件（REQ-061 收口批、REQ-062 issue 过滤、REQ-063 新标发布），用户令 2026-09-23「发个小版本」。semver 判据（R008 一节）：三件全为能力新增面（issue 过滤旗标、publish 新标旗标加 ledger-client 升 v0.1.3、收口命令面重排），取 minor 0.11.0。publish CLI 必填化属破坏面裁定随 minor 滚出（REQ-063 在册：服务端硬校验已先拒同形调用，CLI 侧是诚实快败非新增限制）。

## Criteria

- [x] 版本载体唯一权威：Cargo.toml 0.11.0（release.yml tag 一致性闸对齐）；Cargo.lock 随重生
- [x] CHANGELOG Unreleased 定版 [0.11.0] - 2026-09-23，三 REQ 版本级条目
- [x] 快照与 aidoc 随版本重出（`--llms` 头行与投影版本行，人工审仅版本行差异）
- [x] D45 分支模型：dev/v0.11.0 承载验收（本批工作原在本地 main 未推，挪分支承载，main 回远端态保冻结），CI dev 面绿后 FF 合并 main 打 tag
- [x] 三路验收（R008 二节）：WSL 总台门禁全套、lan-mac 实机三件加 release 构建、lan-ubuntu 实机三件加 release 构建（lan-linux 无克隆，Linux 面按在册 memory 走 lan-ubuntu）
- [x] 发布验收（R008 五节）：run 六 job 绿、资产 10 件、镜像面（latest.json 滚 0.11.0、资产 immutable 头、releases/latest 恒指 v*）、self update 镜像通道报已最新

## Notes

- ROADMAP 阶段表无本批对应阶段变更（ledger 面不在阶段表），不动
- 同 version 禁止重发（R2 immutable 头在册）；资产有误换版本号重发或先 purge
