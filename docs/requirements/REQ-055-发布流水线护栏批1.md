---
id: REQ-055
title: 发布流水线护栏批1
status: draft
priority: must
trace: release.yml 六处与 mirror-models.yml 两处落地;YAML 静态解析绿;dispatch 演练 v0.7.0 实跑 mirror 面与清点步回执待记;首个 tag 实跑全量(含 build 岗自冒烟)后收口 implemented
---

# REQ-055:发布流水线护栏批1

## Scenario

总台对齐单(2026-09-17,project-evo build-release 标准定稿)自查出差距后分批整改;本批为批 1(轻差四件加补缺两件,全在流水线护栏面,不动产物契约与产地架构)。A1(编译产地)与 A2(stable 段形)两项架构级差距待总台裁定,不在本批。

## Criteria

- [x] C1:`gh release create` 补 `--latest` 直发(mirror 末尾 edit --latest 双保险保留)
- [x] C2:两 workflow rclone env 补 `RCLONE_CONFIG_R2_NO_CHECK_BUCKET: "true"`(M015 根因位补禁;隔离目录 copy 手法并存)
- [x] C3:`R2_ENDPOINT` 移入 GitHub Secrets(四键全 Secrets 形;键已 gh secret set 在位)
- [x] C4:Windows 边车 LF 落盘(`[IO.File]::WriteAllText` 单行 LF;消费侧剥 `\r` 兼容保留)
- [x] D1:build 岗打包后原生 OS 解包自冒烟(跑 `--version` 对 tag 逐字;Intel mac 交叉件 Rosetta 缺席降级 file 判形,R005 口径)
- [x] E1:mirror 岗上桶后清点报数与零上传红灯(版本段对象清单与 dist 全等、零对象即红;latest.json max-age=60 头抽验)
- [x] E1b:两处 rclone copy 补 `--ignore-times -v`(存量同内容覆盖 PUT 丢 `--header-upload` 头,2026-09-17 首次演练红灯实证,D42 坑族第三例;修法同 mirror-models.py)
- [ ] dispatch 演练 v0.7.0 验 mirror 面(C2/C3/E1 实跑绿)
- [ ] 首个正式 tag 实跑全量绿(含 build 岗自冒烟五目标)

## Notes

- 对照标准:project-evo main plugins/evo-adr/skills/build-release(七铁则;rust 形)
- 已符合面与架构级差距清单见 2026-09-17 回执(会话面);A1/A2 裁决前不动
