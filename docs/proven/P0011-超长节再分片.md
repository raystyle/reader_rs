# P0011-超长节再分片

> 本方案文档照 `docs\guide\template.md` 写。进行中与否以 `TODO.md` 为准。

- 状态：已完成
- 日期：2026-09-01
- 关联：TODO.md / 前序方案 `docs\proven\P0010-无标题长文档行分片.md`（行分片机制与 200 行预算）

## 背景与问题

P0010 收窄了「无标题文档」的限制，但「有标题文档的超长单节」仍不细分：一份 5000 行的单章文档照样只有一个 `section`，`--pages` 与分页对它失效。用户点名补齐。

## 目标与非目标

- 目标：
  - 有标题文档中**行数超过 200 的节**按同一预算分片为 `part` 单元；短节保持 `section` 不变。
  - 单元号保持全局连续（1 起跨 kind 递增）；kind 标单元来源（page / section / part）。
  - 无标题文档行为与 P0011 前完全一致（全 part）。
- 非目标：
  - 不做节内语义分界（保持与 PDF 页切表格同语义：part 可从任意行界切）。
  - 不做预算旗标。

## 方案

`src\anydoc.rs`：P0010 的分派泛化：`split_markdown` 产节列表后，逐节判定：`len > PART_LINE_BUDGET` 则 `chunk_lines` 产 N 个 part，否则原样一个 section。有标题走此逐节路径；无标题维持整篇 part 路径（小文档单 part 的既有行为与用例不动）。

```text
有标题: [节1(短)=section, 节2(450行)=part×3, 节3(短)=section] → 单元号 1..5
无标题: 整篇 chunk → part×N（不变）
```

## 备选方案

| 方案 | 取舍 |
| --- | --- |
| 超长节内定长切、标签 section N 续（选定），即 part 方案 | 见上；节来源信息由单元顺序隐含，不单列字段 |
| 超长节标 `section N` 多次复用同号 | 单元号须唯一（过滤与 json `no` 字段依赖），弃 |
| 不切 | 用户点名，弃 |

## 实施步骤

1. 立项登记（本文件）。
2. `src\anydoc.rs`：`to_unit_bodies` 纯函数承载逐节判定；单测新添（超长节混排用例）。
3. `tests\cli.rs`：docx 夹具造「短节加 450 行节加短节」，断言 part×3 加 section 混排与单元号连续。
4. 文案：introspect/SKILL/README/CHANGELOG 措辞从「无标题分片」推广为「超长单元分片」。

## 风险与回滚

- 风险：混排 unit 序号与 kind 组合让调用方误解。缓解：SKILL 输出契约写明「单元号全局连续、kind 标来源」；回滚单模块 revert。

## 实施过程与经验

- 实际怎么做：`to_unit_bodies` 纯函数承载逐节判定（短节 section、超预算节 `chunk_lines` 切 part、无标题整篇走 part），`split_markdown` 保持（节列表，有无标题）签名；docx 夹具参数化为 `make_docx_with_body`，混排用例程序化造 450 段。
- 沉淀的经验：分片规则统一到一个纯函数后，P0010 与 P0011 两条规则只剩一个分支差异，「同类限制的推广」落在同一接缝上改动最小。

## 验收标准

- 门禁三件加 rumdl 三件套全绿；既有用例零改动（P0010 行为不变）。[实证: 2026-09-01 15 单元加 43 集成全绿；rumdl 41 文件零告警]
- 新用例：450 行节切为 part×3 混排（section 1、part 2-4、section 5）、`--pages 2` 只出长节首片；无标题 300 行 CSV 用例原样绿。[实证: 2026-09-01 overlong_section_chunks_into_parts_between_sections]
- 真样本：测试V2.docx 仍 15 section；渗透方案单 part。[实证: 2026-09-01 本机]
