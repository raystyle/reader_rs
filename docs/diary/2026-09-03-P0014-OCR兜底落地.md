# 2026-09-03-P0014-OCR兜底落地

> 一天一篇总结自省。方案细节见 `docs\proven\P0014-OCR兜底落地.md`。

## 做了什么

- S006 文档收官提交（ea2b109）：研究文档、日记、三原语；文档门禁补跑全绿。
- P0014 立项并当日达成：`--ocr` 兜底落地（extract 加 search，仅 PDF 单文件 needs_ocr 页）。
  - vendor pure-onnx-ocr 进仓（max_width 2560 修复随带）；hayro 加 ureq 加 sha2 加 prost 加 tract-onnx 加 image 进依赖。
  - `src\ocr.rs`：缓存目录（平台惯例加 `READER_OCR_CACHE_DIR` 覆盖）、三件下载加双套 SHA-256 钉死、进程内 prost strip value_info、hayro 白底渲染、引擎现建。
  - 契约：OCR 回填 lines、`needs_ocr` 标记保留；目录加 `--ocr` 报错；`--offline` 单挂报错。
- 真样本回归：安全牛 PDF 页 1 扫描件与页 2 乱码层 `--ocr` 出正文；无 `--ocr` 行为不变；首用下载全流程（20.5MB 下载加校验加 strip 加缓存加推理）实测打通；search `--ocr` 命中扫描页正文。
- 门禁：16 单元加 46 集成全绿（含模型缓存门控冒烟）；rumdl 45 文件零告警、断链 0、标题括号 0。

## 踩坑

> 三件均已沉淀进 M105 分类文件（M009-M011）。

- M009：ureq 默认响应体上限 10MB，rec 模型 16.6MB 撞限；显式放宽到 64MB。
- M010：vendored 引擎 `println!` 日志污染 stdout（PoC 未暴露），vendor 补丁转 `eprintln!`。教训：PoC 验收口径要含 stdout 纯净度。
- M011：OcrEngine 非 Send/Sync 不能进 static；29ms 构建现建即可。

## 裁决修正

stripped 模型件哈希改钉本进程 prost strip 输出（`a4a307db…` / `0b5e82dc…`），与 S006 PoC 的 Python onnx 序列化字节不同、功能等价（真样本 OCR 输出一致）。

## 自省

- 计划时钉了 PoC 件的 stripped 哈希，没料到 Python 与 prost 的 protobuf 序列化字节差异；首用下载全流程实测当场暴露并修正：全流程回归比单点验证值钱。
- 二进制 7.3MB 涨到 32.9MB，比 S006 估的 15MB 量级还高（hayro 加 tract 加 image 本身重）；已记 P0014 风险节，将来可 feature 拆分。

## P0015 self update

> 当日第二目标，方案 `docs\proven\P0015-self-update.md`。用户点名参考 ohmyenv-rs 与 ohmyagents-rs 加 self update，要求判自身路径。
- 落地：`reader self update [--force]`，stable 单通道（版本判新）；资产 sha256 digest 钉死、缺失拒升；staged 加 rename 原子替换自身（current_exe）与同目录 rr 兄弟；GH_TOKEN 注入、403 回退 gh api；解包 Windows zip crate、其余 tar 加 flate2 1.x。
- 实测：临时目录复制二进制，`self update` 报已最新；`--force` 下载加校验加解包加双名替换全链路，换上件 sha256 与官方 v0.2.1 资产逐一一致、可运行。
- 踩坑 M012：flate2 0.2 无 rust_backend feature 且默认拉 miniz-sys（C 后端），钉 1.x。
- 门禁：19 单元加 47 集成全绿。

## S007 加 P0016 markdown 与 mq 结构化提取

> 当日第三目标。用户点名学习 harehare/mq 加 markdown 搜索与结构化提取。

- S007 研究：mq 双通道核实（MIT、1023 星、活跃）；PoC 实测 `.h`/`.code`/`.link`/select 管道中文样本全对、miette 结构化报错；踩坑 docs.rs 示例过时（`Value` 改名 `RuntimeValue`），以仓内源码为准。
- 裁决：.md 零新依赖复用 split_markdown 进格式面；结构化提取嵌 mq-lang 全引擎；mq-markdown 拍平节点无增量不直用。
- P0016 落地：`.md`/`.markdown` 进 search/extract（节语义与 anydoc 家族同构，批量目录搜索自动覆盖）；`reader query <文件> <mq表达式>` 全格式面（md 原文、anydoc GFM、PDF markdown 管线）；退出码 0/1/2 同 search。
- 测试：23 单元加 52 集成全绿（md 分节/part 分片/query 正负例/json/目录拒绝）；真样本回归五项过。
- 手滑坑：skill curated 文本里 `contains("关键词")` 引号忘转义编译红：format! 系长文本改示例先想转义（已记 P0016 经验节，未够格 M 编号）。

## P0017 OCR 性能优化

> 当日第四目标。用户裁定 19-42 秒/页不可接受，给定 5-10 秒/页。

- 剖析：run_with_metrics 拆三段：det 仅 0.73s、rec infer 19.6s 占 96%。
- 落地：rec 张量动态宽度加 320 桶化、按宽度分组分批、组间 scoped 并行（tract 0.20 单线程无 rayon，每 worker 独立会话，M011 延伸）、rec_batch_size 按核数自适应（16 核档取 2）。
- 实测：第 10 页 20.5s 到 3.0s（32 核）；真样本两页端到端 11.1s（约 5.5 秒/页）达标。质量持平，若干掉字反而修复；水印碎片行由空串变少量噪声短行，已入档。
- 教训：动态宽度不加分组反而更慢（batch 未拆总列数没降）；SVTR 注意力对宽度近似平方级，减宽比减行值钱；PoC 的 bin 要随 vendor 重建（吃过旧二进制误读）。

## 全平台回归

> 用户指出 P0014-P0016 只验了 Windows。推送 main 触发 CI。

- CI run 33623776472 三系统全绿（windows/ubuntu/macos，fmt/clippy/test）。
- 本机交叉 check 受 cc-rs 缺交叉 gcc 拦（zstd-sys 需 C 工具链，P0013 已知件），本机做不了交叉编译，靠 CI：此路径再次实证有效。
- SKILL 重构（用户裁定）：常用例子为主体加渐进引导 `--help`/`--llms`，三节式（常用例子/输出契约/渐进深入）；顺手发现 `cargo test` 不重建 target/debug/reader.exe，SKILL 再生成前必须先 cargo build（流程坑，已纠正）。

## G004 写作规范落地与 OCR 策略明示

> 当日第五、六件事。用户供《中英文 Markdown 技术文档写作规范》转项目规则；并裁定 OCR 性能按核数自适应（核多极限、核少平衡）。

- G004 落盘 `docs\guide\G004-写作规范细则-禁用字符与机械判定.md`：四类 P0 禁令（破折号、箭头、emoji、非法全角）、豁免区、替代写法、自检四步；机检 `md-char-scan.py` 入 .tools（掩豁免区逐字符扫）。
- 存量冲突认账：全仓摸底 205 处违规（绝大多数 `——`），采「豁免基线渐进制」：39 个存量文件入 `md-char-baseline.txt`，清净即除名；新写与改动文档一律须过。文档门禁由三件升四件。
- 坑：Windows 原生 Python 不认 MSYS /tmp（FileNotFoundError），且重定向输出随控制台 GBK 编码，喂回读 utf-8 炸：扫中文文件名清单时要 `PYTHONIOENCODING=utf-8`。
- OCR 策略命名落地：`strategy_tier` 纯函数三档（极限 ≥16 核 batch 2、平衡 8-15 核 batch 4、保守 <8 核 batch 8），单测锚边界；std 无跨平台空闲核数接口，以物理核数为代理并在注释声明不引 sysinfo。

## 全平台实机回归加推送

> 收尾轮：推 main 后 CI 加 lan-linux 与 lan-mac 实机双路回归 P0014-P0017。

- 推送至 8cf02e3；CI run 33623776472 与 33629436533 均三系统全绿。
- lan-mac（arm64）：门禁三件绿（clippy 首跑被 rsproxy 慢传坑一次，重跑过）；release 构建 1m40s；OCR 真样本两页 5.2s、exit 0、输出与 Windows 同型。
- lan-linux（x86_64，12 核）：先卡两坑：默认工具链 1.97.0 对新依赖连爆 rustc ICE（either/autocfg/zlib-rs exit 101），stable 工具链本身损坏（0 字节 cargo、bash_completion 冲突）重装后 1.98.0 过；registry 缓存 parking_lot_core 0.9.12 源码损坏（`unp\`rk_token` 混入反引号），删缓存重解后过。门禁三件绿；OCR 真样本两页 9.7s（batch 4 档）、exit 0。
- 三平台 OCR 实测：Windows 32 核 11.1s/2 页（batch 2）、macOS arm64 5.2s/2 页、Linux 12 核 9.7s/2 页（batch 4），全部进 5-10 秒/页目标档。
- 坑位：registry 源码损坏属 rsproxy 链路缓存损坏新坑；「cargo test 不重建 target/debug bin」与「管道吞退出码（M006 同型）」在 mac 门禁复现一次（clippy 失败被 tail 吞、test 照跑）。

## v0.3.0 封版发布

- 封版 3c1be1c（Cargo.toml 0.3.0、CHANGELOG 封 0.3.0、SKILL 版本重生），tag v0.3.0 推送。
- release run 33631331250 五 job 全绿：musl 目标首验 mq-lang/tract/hayro 静态构建通过（P0014 加依赖时挂的最大风险点消除）。
- 资产验收：10 资产齐；windows zip 官方 .sha256 校验 OK（pwsh 产物带 CRLF 需 tr -d，M008 同族小事）；解包 `reader 0.3.0` 冒烟过；发行件 self update 报已最新（v0.2.1 升 v0.3.0 的实战路径只能等下一版验，self update 是 0.3.0 新能力）。
- 本机 release 与 CI 资产 sha256 不一致（构建机差异；P0013 时 windows/musl 一致不可复现，预期内）。

## S008 加 P0018 OCR 质量升级与换引擎

> 当日第七件事。用户点名「关键是 OCR 质量」。

- S008 四配置同页实测：v5 server 质量好但 125.6s/页出局；v6 tiny（ppocr-rs 原生 CPU 内核）0.8s/页且 mobile 掉字点全修，双优胜出；v6 small 3.2s 更干净留档候选。
- P0018 落地：引擎换 ppocr-rs（git rev 钉 d07857c）；tract/pure-onnx-ocr/prost 出树、vendor 退役；ModelStore 接管模型管理（6.2MB tiny 套件、钉哈希、offline 语义不变）；二进制 32.9MB 回落 28.3MB。
- 质量实情入档：正文掉字全修；水印区噪声行增多加一条幻觉长文；封面大字读散（det 限边 736）。
- 坑：并行 curl 混 cd 只落一件（第二条在仓根执行）；G004 新文档写作又带进 `——` 与 `→`（习惯未改，扫描器当场拦，好门禁）。

## PRD 四原语落地

> 当日第八件事：项目自省。用户指示先学习 `D:\pve-harness` 的 PRD 文档。

- 学习结论：pve-harness 把「需求」独立成四原语之首（PRD 需求清单 / GOAL / PLAN / TODO），需求驱动目标，条目走追问链澄清、D 编号回指派生去向、拒绝记原因防复问。
- 自省四缺口：reader_rs 无权威需求清单（散在 AGENTS 意图路由、GOAL、对话）、无澄清记录、拒绝项（TOON）无统一防复问、GOAL 时间线追溯不到需求源头。
- 落地（D27）：新建 `PRD.md`（生命周期五态加拒绝路径；D01 至 D22 追溯登记 [推断]、D15 TOON 拒绝、D23 至 D26 候选待澄清）；AGENTS 工作节奏改四原语加「禁止静默假设」、意图路由候选方向挪入 PRD；INDEX 前缀与目录树、R007 登记步同步。
- 分工定义经用户逐条裁定：PRD 是需求清单；GOAL 是理解 PRD 后定下的目标和达成标准；PLAN 是规划（怎么做）；TODO 是做的进度。

## 六层测试规范与 A/B 层落地

> 当日第九件事。用户三连指示：定单元/集成/冒烟/回归/验收/A/B 六层测试标准规范；补齐 A/B 测试目录并翻入对象资源跑质量与性能对比；研究产物落 poc 目录。

- PRD 登记 D28 / D29 / D30 并当轮交付。
- G006 落盘：六层各定测什么、落点、什么叫过、何时跑；真样本行为基线登记成表（安全牛 PDF 25 命中加第 10 页 OCR 检查点、合成 scan-cjk）。
- D29 实现：`tests\ab\`（manifest 加 expectations 加 assets 加 reports）加 `.tools\ab_run.py`；合成样本生成器 `make-scan-sample.py`（Pillow 直出无文本层 PDF，检查点独立来源为渲染源文本）；`READER_OCR_MODEL_SIZE` env 开关进 src\ocr.rs（tiny 默认，small 可选，带 parse 单元测试）。
- A/B 首跑（tiny vs small，32 核 release）：真样本第 10 页 must 5/5 对 5/5、行召回 37 对 51、1.99s 对 5.13s/页，与 S008 PoC 数据吻合；合成干净样本 tiny 仅 1/5 命中而 small 4/5，tiny 对干净混排文本反而掉内容，成 D25 质量档的新证据。
- 跑批器两处当场修正：首跑把 small 模型下载计进 wall（30.8s 假象）改先热跑再计时；检查点匹配改去空白后比较（OCR 在中英文与数字两侧加空格属排版噪声，首跑「高达 287 天」因空格误报缺失）。
- D30 落地：`poc\` 目录约定（源码清单入仓、产物模型 gitignore、上游 clone 只记指针）；S006 PoC 源码自 target\poc-ocr 迁入 poc\s006-ocr-mobile\；G002 增八节。

## 归类修正：references 流程、guide 规范

> 当日第十件事。用户裁定目录定义：references 是做事的流程，guide 是做事的规范。

- 三件错位搬家：R003 测试标准细则（规范）改 G005 入 guide；R006 测试体系细则（规范）改 G006；G003 工作流五步（流程）改 R007 入 references。git mv 加 md-replace 字面映射，16 文件 56 处引用一遍过。
- INDEX 六/七节行归位、节名与职能描述改写（references：做事的流程；guide：做事的规范）、退役编号注记（R003 / R006 / G003 不复用）；AGENTS 速记同步。

## 测试程序统一 uv 运行时 Python 载体

> 当日第十一件事。用户裁定：测试程序用 uv 运行时 Python 脚本写（D31）。

- 三层跑批脚本落地并全绿：`tests\smoke.py`（5 项冒烟：version/help/search 双向退出码/扫描件 needs_ocr）、`tests\regress.py`（4 项真样本基线：CLR 书 search assert_cmd 25 命中与 extract 399 页标记、安全牛 81 页标记、scan-cjk needs_ocr）、`tests\accept.py`（5 项可机检验收：版本一致、--llms、json 包膜、退出码档、query）。
- G006 增载体规则：单元/集成归 cargo，冒烟/回归/验收/A/B 归 uv 运行时 Python（PEP 723 单文件、stdlib 优先、退出码 0/1/2）。
- 当场修正 G006 基线归因错误：「25 命中」属 CLR 书（S001）非安全牛 PDF；安全牛基线实为 81 页标记。写回归脚本时的实测对照逼出了这个文档错误，好流程。
- 坑复用：Windows 二进制不认 MSYS /d/ 路径（M102 同型），真样本路径一律 Windows 形式；head 截断管道会 SIGPIPE 截断 tee 落盘（M007 同型），计数别用 head 管道。

## AGENTS 自省与六层框架修正

> 当日第十二件事。用户供稿《Rust 规范测试流程》并点名 AGENTS 重新自省：调整各目录和文档职责、规范、规则和意图路由（D32）。

- 框架修正入 G006 一节：Rust 原生内置只有单元/集成/文档测试三层（cargo test 顺序门禁：前段失败挡住后段）；冒烟/回归/验收/A/B 是目的流程层，无官方框架，靠组织约定加 uv 脚本拼装。供稿的在线实验部分（分桶 SDK、灰度）裁定不适用于本 CLI 形态，本仓 A/B 定位为离线筛选门。
- 采纳要点：回归集唯一正规生长方式（修 bug 必带复现用例）；文档测试与 examples 列演进候选；生态件 insta/proptest/trybuild/cucumber 入演进候选表（不预先装）。
- AGENTS 调整：规则 1 四原语带职责定义；规则 10 补 uv 载体规则；四节增目录职责总表（docs 六目录加 poc、tests、.tools）；分析路径按「流程/规范」分工改写。

## D31 第 2 轮：三层归 cargo，uv 只留 A/B

> 当日第十三件事。用户裁定收窄：A/B 测试用 uv 运行时 Python 脚本，其它层用 cargo test 体系。

- 三层改写为独立 test target：`tests\smoke.rs`（3 测）、`tests\regress.rs`（4 测，external 样本缺失或 sha256 不符跳过）、`tests\accept.rs`（5 测，serde_json 解包膜、sha2 核样本哈希进 dev-dependencies）；`cargo test --test smoke/regress/accept` 单独调度，12 测全绿。
- py 三脚本删除；G006 载体规则改写（cargo 五层加 uv 仅 A/B）；AGENTS 规则 10 与意图路由、INDEX、PRD 同步。
- 自省：第 1 轮把四层全铺 uv 是过度套用新玩具；cargo test 体系自带调度与门禁集成，流程层只有「对比跑批非断言」的 A/B 真正需要脚本灵活性。

## 验收层 BDD 化

> 当日第十四件事。用户点名供稿流水线表中的「发布前：验收/BDD 测试（cucumber + testcontainers-rs）」（D33）。

- 裁定拆分：cucumber 落地；testcontainers-rs 不适用（纯 CLI，无可容器化依赖），入 G006 边界注记。
- 落地：cucumber 0.23 加 futures 进 dev-dependencies；`tests\features\accept.feature` 8 场景业务语言场景；`tests\accept.rs` 改为步骤绑定（World 存 code/stdout/stderr）；`[[test]] harness = false` 让 cucumber 自管输出与退出码。8 场景 21 步首跑全绿。
- 坑两枚入档：cucumber 0.23 步骤函数首参必须 `&mut World`（then 也不例外）；`[[test]]` 表插进 dev-dependencies 中间把 rbook/zip 吞成 test 表键（M013，TOML 节序即语义）。

## 回归层增强：insta 快照加 proptest 属性

> 当日第十五件事。用户点名供稿流水线表中的「合并后：回归集加属性/快照测试（insta、proptest、trybuild）」（D34）。

- insta 落地：`tests\snapshot.rs` 3 快照（合成双页 PDF 的 extract 全量输出、search 命中格式、--llms 索引全文）；快照入仓 `tests\snapshots\`；首跑按 insta 纪律以失败产出 .snap.new，逐个人工审内容后改名入库（--llms 快照含版本号，发版 bump 会自然触发复审，好守卫）。
- proptest 落地：页范围解析 3 属性（单页集合往返、区间覆盖、任何含 0 串拒绝），进 lib.rs 单元测试模块。
- trybuild 裁定不适用：公开面是 CLI 不是库 API，无编译期契约要守，入 G006 演进表。
- 坑两枚：快照两测试同进程同 pid 共用临时 PDF 路径，并行互删（NotFound），改按用例分名；proptest 宏内 format! 不能捕获外层变量（先绑定再进宏）。

## Rust CLI 工程基线供稿消化

> 当日第十六件事。用户供稿《现代 Rust CLI 共识》（库先行、clap derive、进程级测试隔离、跨平台矩阵发布）（D35）。

- 处理方式定为「逐项裁定」而非全量照搬：G007 落盘，四态（已符合 6 / 已落地 1 / 候选 4 / 不适用 3）。
- 已符合：库先行薄壳、clap derive、路径与 cfg 纪律、ureq 纯 Rust、五 job 目标矩阵、六层测试。
- 已落地：release profile `strip = "symbols"` 加 `lto = "thin"`；codegen-units = 1 不采纳（构建变慢收益小）。
- 候选入 PRD：D36 错误与诊断体系（thiserror/anyhow/tracing）、D37 发布面（aarch64 musl、cargo-dist）、D38 trycmd、D39 工程效率件（edition 2024 钉 toolchain、nextest、llvm-cov、deny/audit），各带触发条件。
- 不适用防复问：testcontainers/wiremock（无服务依赖）、trybuild（非库 API）、TUI 测试（无交互）。

## AGENTS 定位节与目录职责修正

> 当日第十七件事。用户指出两处：管理对象应改交互对象；方案索引与资源索引重复、references 与 proven 职责不清。

- 交互对象：AGENTS 一节第 3 条改名并顺语义（只读交互，不渲染不编辑）。
- 去重：AGENTS 一.4 方案索引整节撤掉（与四节资源索引、INDEX 重复维护），只留 R001 定位展开单指针加一句「历史方案与研究一律查 INDEX」。
- 职责划清：proven 是已完成方案的历史归档（封存，不再更新）；references 是现役做事的流程（持续更新，下次照着做）；分界句「方案做成归档进 proven，可复用流程提炼进 references，标准禁令进 guide」写进 AGENTS 四节、INDEX 一节与 G002 角色表。

## AGENTS 补 poc 与测试资源说明

> 当日第十八件事。用户指出 AGENTS 没有说明 poc 研究原型产物和 tests 测试资源目录。

- 新增规则 12「做研究出原型时」：PoC 落 `poc\<S编号-主题>\`（源码清单入仓、产物模型 gitignore、上游 clone 只记指针、结论回填 S 文档）；禁止散落 target 临时工区、大件进 git、有 PoC 无结论。
- 规则 10 补测试资源落点：夹具现造入临时目录；入仓资产只放 `tests\assets\` 与 `tests\ab\assets\`；外部真样本不入仓、钉 sha256、缺失即跳过；禁外部版权样本进仓。

## AGENTS 二节改名工作规则并重组四类

> 当日第十九件事。用户裁定：操作规则改工作规则，分类为对话、操作、文档、编码。

- 二节重组：对话（每轮对话）、操作（踩坑/发现问题/交付变更/提交）、文档（写文档/写研究与测试文档）、编码（命令与文件/Rust/测试/临时脚本/研究原型）；12 条规则重排不改动内容。
- 引用同步：G001、G005、R007 三处「操作规则」改「工作规则」；diary 历史记述不动。

## 工作规则动作化重构

> 当日第二十件事。用户裁定二节结构：对话/操作/编码三类先列动作清单再定规则（可以/禁止/参考），文档类改为动作后的对齐义务表（什么动作、到什么状态、写什么文档）。

- 动作清单：对话 5 项（新需求、追问、裁定验收、踩坑上报、问题发现）；操作 5 项（执行命令与文件、跑门禁、跑脚本工具、git 变更、发布 tag）；编码 5 项（写改源码、依赖与构建配置、写测试、写脚本、PoC 原型）。
- 文档类对齐义务表 13 行：从「新需求提出，PRD 登记」到「发布 tag，CHANGELOG 封版」，每个动作挂状态时机与必写文档；写作标准（G001/G004/六态）保留为类内规则。
- 引用回扫：R007 的「发现问题时」锚点改指对话类每轮对话；G001 改指文档类写作标准；G005 改指编码类写测试时。

## AGENTS 撤意图路由节

> 当日第二十一件事。用户裁定：意图路由节删除。

- 三节整节撤掉（命令面以 README / SKILL 为准，候选与已落地归 PRD）；AGENTS 收成三段职责：项目定位、工作规则、文档索引；原文档索引节升为三节。
- 防丢兜底：门禁命令指针补进操作规则 2 参考行（cargo 三件见 G005、文档四件见 G004/INDEX）；发布流程指针本就在规则 3；文档对齐表的「意图路由同步」改为「README / SKILL 同步（命令面变化重生 SKILL）」。
