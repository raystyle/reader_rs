# 2026-09-19 issue 列表截断同型修复

> 来源：上游缺陷档案 issues.ohmygh.com #52（browse-rs 立单，browse 工位修复中），本仓排查同型缺陷并修。

## 概貌

自查命中同型三件：CLI 默认 limit=50（lib.rs clap 定义，低于服务端上限 100，open 集超 50 后旧条目默认面隐形，静默截断与 browse 同型）；返回条数打满 limit 零提示；count 语义（本次返回条数非在册总数）未写明。修法照 browse 配方等价落地：默认提至 100（help 面自动带 `[default: 100]`）；`run_issue_list` 返回条数等于 clamp 后上限时 stderr 一行截断提示（指路 `--status`/`--tool` 收窄或网页面），stdout 保纯数据、两形态同示；List 命令描述与 `--limit` 帮助写明默认值、上限与 count 语义，README 与 `--llms` curated 行同步（三处同步纪律），手册快照重出。测试加 `list_saturation_hint_cli_face`：复用本 target 一次性 HTTP 服务基建走 CLI 面断言（打满出提示、不满不出），`--limit 1` 对恰好 1 条在册同提示属语义正确（无法区分还有没有更多）。

## 实弹

`reader issue list --limit 1` 对真 API：stdout 首行恰为上游 #52 本单（新到旧第一名），stderr 出提示行原文「reader: 返回条数已达上限 1（可能截断）；--status 或 --tool 过滤收窄，或网页面 issues.ohmygh.com 看全量」，exit 0 [实证: 2026-09-19 本机 release 件输出]。坑一枚：tests/issue.rs 引 predicates 断言 `.not()` 需 `predicates::prelude::*` 入域（ContainsPredicate 无布尔扩展方法）。

## 得失

- 得：截断面从静默变明示，默认面开到服务端上限；同型缺陷随上游 #52 批次闭环
- 失：无（不改 json 形状与退出码契约）

## 增记 #53 家族统一增量

上游 #53（browse 0167e8c 参照，服务端契约已部署生产）补齐剩余两点：`--before <id>` keyset 游标（`issue::list` 加参透传，回 `ListPage { rows, has_more }`；带游标的回执含 has_more 布尔，json 面随 data 透出，非法值服务端 400 透传归因；clap 取 u64 形，非数字在客户端即拒）；饱和提示双出口统一（带游标走 has_more 精确判定出「更早仍有条目」行，旧形回执保持打满启发式，两行文案都含 `--before` 翻页出口）。json 形状加性扩展（`has_more` 仅带游标时在场），count 语义与退出码契约零变化。实弹三验（release 件对生产）：`--limit 3 --before 51` 取到 [50,49,48] 更旧一页加 has_more 行、json 出 `"has_more":true`、`--limit 3` 饱和行含 `--before <id>` 出口 [实证: 2026-09-19 本机输出]。漂移守卫随活树自动盖 `--before`（手册表旗标列自渲染）。

## 增记 封版 v0.9.1

总台令收 issue 客户端批（基线核：tag v0.9.0 后 main 加三笔，树净）。dev/v0.9.1 承载封版件（Cargo.toml 0.9.1、CHANGELOG 定版、快照与 aidoc 随版本重出），三路门禁全绿（本机 11 件、双实机各四件、dev CI run 35445476688 success）后 FF 合并 main 打 tag。release run 35445639421 六 job 绿（含 mirror 岗）；资产 10 件（五平台 × 包加 sha256 边车）；镜像面：latest.json 广告 0.9.1（逐平台 sha256 与官方 digest 逐字等，max-age=60 头）、0.9.1 资产 immutable 头 [实证: gh api 与 curl 源站头]。发行件冒烟：边车校验 OK、双名 0.9.1、--before 翻页行在、self update 报已是最新 [实证: /tmp/r091 解包实测]。发毕知会总台滚 catalog。
