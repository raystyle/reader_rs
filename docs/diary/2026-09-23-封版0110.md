# 2026-09-23 封版 0.11.0

> 来源：用户令「发个小版本」。Unreleased 三件（REQ-061 收口、REQ-062 issue 过滤、REQ-063 新标发布）定版 0.11.0，D45 分支模型承载。

## 概貌

semver 判据（R008）：三件全为能力新增面取 minor。本批工作（079e254、1847f8b、d6d602c）原在本地 main 未推，按 D45 挪 `dev/v0.11.0` 分支承载（main reset 回远端态 a103b0b 保冻结，无损移动）。封版件：Cargo.toml 0.11.0（载体唯一权威）、Cargo.lock 随重生、CHANGELOG 定版（三 REQ 版本级条目加判据头注）、`--llms` 快照与 aidoc 投影版本行随出（人工审仅版本行差异）、REQ-064 登记。三路验收（WSL 全门禁、lan-mac、lan-ubuntu；lan-linux 无克隆按在册 memory 走 lan-ubuntu 承载 Linux 面）绿后 FF 合并 main、tag v0.11.0 推送触发 release.yml（五平台资产加 sha256 边车加 mirror 腿）。

## 验收记录

- 三路门禁（2026-09-23，R008 二节）：
  - WSL 总台：fmt 加 clippy -D warnings 加 `cargo test --locked`（12 target 全 ok）加 doctest 4 加 rumdl 89 件加 md 三扫加 `cargo aidoc --check --strict` 加 PEVO 11 PASS 加 release 构建，退出码全 0 [实证: 当日各命令输出]
  - lan-mac：dev/v0.11.0（cdf7aae）门禁三件加 release 构建绿，`reader 0.11.0` 冒烟 [实证: ssh 阶段标记输出]
  - lan-ubuntu：同绿；注意其 remote refspec 只跟 main（`+refs/heads/main:...`），dev 分支需显式 `git fetch origin dev/v0.11.0` 后从 FETCH_HEAD 建支；cargo 需补 `~/.cargo/bin` PATH（memory 在册吻合）[实证: ssh 阶段标记输出]
  - CI dev 面：run 35854960397 三系统 success [实证: gh run view conclusion]
- 发布（R008 四节）：main FF 至 cdf7aae、tag v0.11.0 推送触发 release.yml；dev 分支发布后即删（本地加远端）
- 发布验收（R008 五节）：run 35855550081 六 job 绿（五平台 build 加 mirror）[实证: gh jobs conclusion]；资产 10 件齐（五平台 zip/tar.gz 加 .sha256 边车）；镜像面：latest.json HTTP 200 加 cache-control max-age=60 加 version 0.11.0 加五平台清单、0.11.0 win 资产（29428119B）immutable 头 [实证: curl -sI 输出]；linux-gnu 件 sha256 边车逐字节一致（20b885bf...）加解包冒烟 `reader 0.11.0`；发行件 `self update` 镜像通道报 current 0.11.0 已最新 [实证: 命令输出]
- 本机工具面：self update 被 ark-managed 布局拦截（REQ-059 在册预期），走 `ark update reader` 滚 0.11.0 成功，`reader --version` = 0.11.0

## 门禁

封版件提交 cdf7aae（dev/v0.11.0 承载）全门禁绿后 FF；发布链 main CI 与 release 六 job 绿；发行后回填本档（先例 6f502fe 口径）。
