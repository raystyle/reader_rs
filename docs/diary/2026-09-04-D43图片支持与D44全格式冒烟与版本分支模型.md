# 2026-09-04-D43图片支持与D44全格式冒烟与版本分支模型

> 一天一篇总结自省。研究细节见 `docs\research\S009`；分支模型裁定见 PRD D45。

## 做了什么

- D42 收尾 bug 修复（M017，ohmycloud 大陆侧验收回执上报）：`ocr init` 对不存在缓存目录三通道 `os error 3`；建目录归 `download_file` 单一权责（落盘前 `create_dir_all(parent)`，标准库即安全全平台实现，零新库），`prefetch_pair` 冗余建目录删；回归 `tests\mirror.rs`（本机一次性 std HTTP 服务加合成 pin，独立 target 隔离 `READER_MIRROR` 进程内 env；stash 掉修复实测红态复现）。
- D43 图片文件支持当日立项并落地（S009 四裁：八扩展名、--ocr opt-in 同 PDF 契约、多帧首帧、单图即 page 1）：`ocr_image`（内容嗅探、EXIF 方向 `orientation()` 加 `apply_orientation`、alpha 合成白底、`recognize` 直吃）；`build_engine` 从 `ocr_pages` 抽出共用（PDF 行为零变化）；document 分派与 `is_supported` 真源；query 拒图片指路 --ocr；GDI+ 文字图端到端全识（`READER SMOKE 12345`）；cli 集成 7 例含门控 OCR 端到端。
- D44 全格式冒烟：覆盖矩阵实证 7 族零覆盖后，用户裁定取 anydoc 官方测试用例；`tests\assets\anydoc\` 九族入仓（firecrawl/anydoc@261fc25，MIT，sha256 钉目录 README）；ppt 二进制族与 xls / xlsb 现造不出的缺口直接消灭；smoke 全格式活体 5 项 0.3 秒全绿，CI 无模型无 Word 全跑。
- D45 版本分支模型（用户裁定）：`main` 稳定主干唯一发版源，`dev/v<版本>` 承载开发与验收（状态隔离），CI 加 `dev/**` 触发，验收全绿 fast-forward 合并 main 打 tag 发布，发版窗口 main 冻结；AGENTS 规则 3 与 R008 同步；首条分支 `dev/v0.6.0` 承载当日分组提交。
- 文档对齐：S009 落盘、PRD D43 / D44 / D45 已采纳、AGENTS 边界（图片入支持面）、README / SKILL 重生 / introspect / G006 冒烟落点 / INDEX 全同步；门禁全绿（cargo 三件 118 例、rumdl 66 文件、md 三扫零违规）。

## 踩坑

> 已沉淀 M105（M017）与快照纪律执行。

- M017：`fs::write` 不建父目录；下载器依赖调用方建目录就必踩（`extract --ocr` 有人建、`ocr init` 没人建）。单一权责归下载器。
- 自踩两枚：`filter.is_some_and` 反转（`None` 全量被误判过滤掉，真机契约验证当场暴露）；ODF draw 命名空间 URN 是 `drawing:1.0` 不是 `draw:1.0`（把前缀当 URN），anydoc 的 ns 常量对照后修正。
- `--llms` insta 快照过时：diff 人工审（仅两处预期 curated 变更）后收录，走快照纪律不盲 accept。

## 自省

- 「研究下」先出研究结论与四裁再动手，避免了 avif / 冷门格式提前铺承诺面；官方测试用例的指示直接把现造不出的 ppt 族缺口变成入仓语料，比我原计划的门控跳过高一档。
- 分支模型补在发版前正好：dev/v0.6.0 首推即三系统 CI 绿（分支触发面实战验证）。
- 镜像分发幂等（用户裁定）：周更原来每周白下 HF 36MB、`--ignore-times` 强制重传全部模型件、`gh --clobber` 重传资产，`mirrored_at` 还让清单永不相等；幂等闸用「清单核心排除 volatile 字段比对」解决，远端不可读按有变化走全量防误跳发布。本机对真远端实跑 NO-CHANGE；客户端侧 `ocr init` 完整缓存死镜像证零下载。self update 回归面核对：版本比较、坏清单形状拒用、平台映射单测在位，真升级路径按 P0015 裁定不进 CI（记端到端验收）。
- 测试面再扩（用户两条点名）：「测试文档与用例太少」:anydoc corpus 从 9 件活体扩为 71 件全量（含 malformed 负例与 abuse 滥用件，上游文件名即期望行为编码），tests\corpus.rs 63 快照加负例加滥用断言；本地语料主性能与质量面落 manifest 钉 sha256 四模式工具加 gated 基线核验（外部样本不入仓，性能归报告不进断言）。快照跨机坑当场抓到：stderr 带夹具路径，只换仓根前缀残留 OS 分隔符差异（`\` 对 `/`）CI 必炸，整串归一 `<fixture>` 解决（首跑 CI 红实证后修）。
- D46 第 2 轮（用户裁定）：弃 E:\ebook（172 件 2.26GB 基线废弃）改 E:\研究资料（575 件 5.4GB，551 pdf 研究论文加 pptx/epub/docx 等），机制平移改名 materials（`.tools\materials-corpus.py` 加 tests\materials.rs），默认根与 env（READER_MATERIALS_ROOT/BIN）随之换名。
- D47 图片本体（用户追问「图 1-1 呢」引出，两轮定界「只负责提取存储图片，和文本元数据对齐」）：S010 三层路线（T1 figures 落地 / T2 PoC 先行 / T3 模型已拒防复问）；figures 四路实测（研究资料书页 310KB PNG 落盘、md 引用、docx media、图片文件）；PoC 实测年份轴融合串切分成立、数值配列两难点未达精度（变长小数粘串、双系列列偏移）暂不进主仓。自踩：sanitize 全 ASCII 化把中文书名变全下划线撞名（改留 CJK）、zip 锚分隔符跨机漂（归一正斜杠）、图题判定「如图1-2所示」误中（改行首约束）。
